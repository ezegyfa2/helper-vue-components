section.page-section.bg-light(v-bind:id='id')
    .container
        dynamic-component(
            v-bind:component_infos='title_section'
        )
        .row.justify-content-center
            .col-lg-3.col-md-4.col-sm-6(v-for="member in member_sections")
                dynamic-component(
                    v-bind:component_infos='member'
                )
        .row(v-if='footer_text')
            .col-lg-8.mx-auto.mt-3.mb-5.text-center
                p.large.text-muted
                    | {{ footer_text }}
